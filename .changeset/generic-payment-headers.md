---
"blossom-client-sdk": minor
---

Add generic `onPaymentRequired` handlers for HTTP 402 retries and remove the optional Cashu peer dependency. The legacy `onPayment` Cashu handlers remain supported but are deprecated.
