---
phase: 02-bud-15-chk-and-secret-safe-references
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/hashtree/blossom-reference.ts
  - src/hashtree/types.ts
  - tests/hashtree/blossom-reference.test.ts
  - tests/hashtree/secret-safety.test.ts
  - tests/hashtree/fixtures/leaky-diagnostic.fixture.ts
autonomous: true
requirements: [CHK-04, CHK-05, CHK-06]
must_haves:
  truths:
    - "CHK-04/D-06/D-09: encrypted Blossom references require exactly one `enc=chk-v1` and one lowercase-hex 32-byte `k`; missing, duplicate, conflicting, or unsupported security fields are rejected."
    - "D-07/D-08: parse/build preserves repeated unknown extensions and canonicalizes recognized fields as enc,k,xs,as,sz followed by extensions sorted by decoded key/value with stable ties."
    - "CHK-04: parse→build is idempotent after canonicalization, including empty extension values and repeated fields, and pure concurrent calls do not share state."
    - "CHK-05/D-11/D-12: parsed encrypted references are explicitly capability-bearing, while diagnostic/progress contracts contain public metadata only and cannot accept `k`, plaintext, or a complete encrypted URI."
    - "CHK-06/D-04/D-05: plaintext and chk-v1 are first-class discriminants, and an absent future client mode means plaintext without introducing a combined crypto function."
    - "EDGE-CHK-04-IDEMPOTENCY: parsing and rebuilding an already canonical Blossom reference produces the same canonical reference on every repetition."
    - "EDGE-CHK-04-CONCURRENCY: parallel reference parse/build operations are pure, share no mutable state, and preserve the same canonical results as isolated calls."
  artifacts:
    - path: "src/hashtree/blossom-reference.ts"
      provides: "Strict, lossless, capability-bearing Blossom reference parser/builder"
      exports: ["parseHashtreeBlossomReference", "buildHashtreeBlossomReference"]
    - path: "src/hashtree/types.ts"
      provides: "Plaintext-default mode and secret-safe diagnostic/progress contracts"
    - path: "tests/hashtree/blossom-reference.test.ts"
      provides: "Cardinality, encoding, preservation, canonicalization, idempotency, and concurrency proofs"
  key_links:
    - from: "src/hashtree/blossom-reference.ts"
      to: "src/hashtree/errors.ts"
      via: "strict public-field validation uses HashtreeValidationError"
      pattern: "HashtreeValidationError"
    - from: "src/hashtree/blossom-reference.ts"
      to: "src/hashtree/chk.ts"
      via: "reference key/hash byte lengths match the CHK primitive contract without invoking crypto"
      pattern: "Uint8Array"
  assumptions:
    - id: "EDGE-CHK-05-UNCLASSIFIED"
      statement: "Flagged/unverified: the explicit server, error, progress, and diagnostic leak boundaries are the complete secret-exposure boundary for CHK-05; the generic edge probe supplied no narrower classified edge."
    - id: "EDGE-CHK-06-UNCLASSIFIED"
      statement: "Flagged/unverified: the discriminated plaintext/chk-v1 contract and absent-mode plaintext client default completely represent CHK-06 in this phase; the generic edge probe supplied no narrower classified edge."
  prohibitions:
    - id: "P-CHK-02"
      requirement_id: "CHK-05"
      category: "privacy"
      status: "unverified"
      verification: null
      flagged: true
      statement: "Routine progress and diagnostics must not contain plaintext, key bytes, complete encrypted URIs, or parsed capability-bearing reference objects."
---

<objective>
Add strict, lossless encrypted Blossom references and secret-safe plaintext/encrypted mode contracts on top of the verified CHK core (CHK-04, CHK-05, CHK-06; D-04–D-12).

Purpose: Let callers transport CHK capabilities without ambiguous security fields or accidentally widening where secrets may flow.
Output: Capability-aware reference parser/builder, shared mode/diagnostic types, and adversarial tests.
</objective>

<execution_context>
@/home/user/.codex/gsd-core/workflows/execute-plan.md
@/home/user/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bud-15-chk-and-secret-safe-references/02-CONTEXT.md
@.planning/phases/02-bud-15-chk-and-secret-safe-references/02-RESEARCH.md
@.planning/phases/02-bud-15-chk-and-secret-safe-references/02-PATTERNS.md
@.planning/phases/02-bud-15-chk-and-secret-safe-references/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse and canonically rebuild capability-bearing Blossom references</name>
  <read_first>
    <file>src/helpers/blossom-uri.ts</file>
    <file>tests/helpers/blossom-uri.test.ts</file>
    <file>src/hashtree/errors.ts</file>
    <file>src/hashtree/chk.ts</file>
    <file>.planning/phases/02-bud-15-chk-and-secret-safe-references/02-CONTEXT.md</file>
  </read_first>
  <files>src/hashtree/blossom-reference.ts, tests/hashtree/blossom-reference.test.ts</files>
  <behavior>
    - Plain references retain public hash/ext/xs/as/sz metadata; encrypted references additionally expose `mode: "chk-v1"` and an independent enumerable 32-byte key per D-06/D-11.
    - Encrypted input accepts exactly one `enc=chk-v1` and one `k` of 64 lowercase hex characters; every missing, duplicate, conflicting, uppercase, short, non-hex, or unsupported case throws HashtreeValidationError per D-09.
    - Unknown query pairs, including repeats and empty values, survive parse/build; builders output enc,k,xs,as,sz then decoded-key/value sorted extensions with stable ties per D-07/D-08.
    - Rebuilding a parsed canonical reference is idempotent, `xs`/`as` preserve relative order, `sz` remains addressed-blob size, and concurrent parses/builds are independent.
  </behavior>
  <action>Write failing positive/negative/round-trip tests, then create discriminated `PlaintextBlossomReference` and `EncryptedBlossomReference` plain-data types plus `parseHashtreeBlossomReference(uri: string)` and `buildHashtreeBlossomReference(reference)` in the isolated module. Reuse the existing `blossom:<sha256>.<ext>` and BUD-10 xs/as/sz semantics without modifying `src/helpers/blossom-uri.ts`. Iterate `URLSearchParams` entries as ordered pairs so repeats survive; represent extensions as readonly `{ key, value }[]`, not a record/map. Per D-06–D-09 validate builder and parser inputs symmetrically, require singleton recognized security fields, reject unsupported `enc`, decode/encode `k` at exactly 32 bytes, copy key bytes, and use `HashtreeValidationError`. Lock project discretion to canonical order `enc,k,xs,as,sz`, then stable-sort extension pairs by decoded key and value. Test percent-encoding, empty values, adjacency, ordering, idempotency, and Promise.all purity. Document in TSDoc that the encrypted object is an enumerable bearer capability unsafe for indiscriminate logging/serialization per D-11.</action>
  <verify>
    <automated>pnpm vitest run tests/hashtree/blossom-reference.test.ts -x &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>Valid plaintext/encrypted references parse to typed independent data, every ambiguous security-query shape fails, repeated extensions survive, and canonical parse/build output is stable across repeated and concurrent calls.</acceptance_criteria>
  <done>CHK-04 references round-trip through a strict singleton parser and deterministic lossless builder in Node tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish plaintext-default and secret-safe callback contracts</name>
  <read_first>
    <file>src/hashtree/types.ts</file>
    <file>src/hashtree/blossom-reference.ts</file>
    <file>tests/hashtree/exports.test.ts</file>
    <file>tests/hashtree/secret-safety.test.ts</file>
    <file>.planning/phases/02-bud-15-chk-and-secret-safe-references/02-CONTEXT.md</file>
  </read_first>
  <files>src/hashtree/types.ts, tests/hashtree/secret-safety.test.ts, tests/hashtree/fixtures/leaky-diagnostic.fixture.ts</files>
  <behavior>
    - `HashtreeMode` is `"plaintext" | "chk-v1"`; `HashtreeModeOptions` permits omitted mode only on the plaintext branch, encoding the later client default per D-05.
    - Progress/diagnostic payloads can carry operation, byte counts, ciphertext hash, and non-secret mode, but their public types expose no plaintext/key/URI/reference field per D-12.
    - Runtime sentinel inspection proves callback payloads and errors contain none of the secret values, while parsed encrypted references deliberately enumerate `key` per D-11.
  </behavior>
  <action>Add readonly portable `HashtreeMode`, discriminated `HashtreeModeOptions`, `HashtreePublicProgress`, and `HashtreeDiagnostic` contracts to `src/hashtree/types.ts`. Treat absent `mode` as the typed plaintext default for future clients per CHK-06/D-05, but do not implement a class or a mode-aware crypto/pass-through function per D-04. Limit callback fields to operation, processed/total byte counts, copied ciphertext hash, and non-secret mode; do not accept a generic metadata/context bag. Expand secret-safety tests with unique plaintext, key, and full-URI sentinels, recursive payload/error/JSON inspection, a deliberately leaky fixture proving the negative assertion fails, and type-level `@ts-expect-error` assignments for forbidden fields. Confirm parsed capability objects remain ordinary enumerable data while never being accepted as routine callback payloads.</action>
  <verify>
    <automated>pnpm vitest run tests/hashtree/secret-safety.test.ts tests/hashtree/blossom-reference.test.ts -x &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>Mode types encode plaintext as the omitted default and encrypted mode explicitly; diagnostics/progress accept only the named public metadata; both runtime and compiler tests reject capability leakage without hiding the parsed key.</acceptance_criteria>
  <done>CHK-05/CHK-06 contracts compile and their fail-first runtime safety checks pass without a combined crypto API or stateful client implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URI text → typed reference | Untrusted query strings become capability-bearing application data. |
| capability object → callback/diagnostic | Bearer key material must not cross into routine observability channels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-05 | Spoofing/Tampering | reference parser | high | mitigate | Require exact singleton enc/k and reject unsupported/duplicate/conflicting fields. |
| T-02-06 | Information Disclosure | parsed reference and callbacks | high | mitigate | Mark object capability-bearing; narrow callback types and use recursive secret-sentinel tests. |
| T-02-07 | Tampering | reference builder | medium | mitigate | Symmetric validation and deterministic recognized/extension ordering with lossless repeats. |
| T-02-08 | Repudiation | canonicalization | low | mitigate | Idempotency and concurrent-purity tests make emitted reference form reproducible. |
| T-02-SC | Tampering | npm installs | high | mitigate | This plan adds no package dependency or install task. |
</threat_model>

<verification>
Run `pnpm vitest run tests/hashtree/blossom-reference.test.ts tests/hashtree/secret-safety.test.ts &amp;&amp; pnpm build`, then repeat both behavioral files with `pnpm vitest run --browser --browser.headless`.
</verification>

<success_criteria>
Reference parsing/building is strict for security fields, lossless for extension fields, deterministic and idempotent; public observability contracts cannot carry secrets; plaintext is encoded as the future client default.
</success_criteria>

## Artifacts this phase produces

- `src/hashtree/blossom-reference.ts`: reference types, `parseHashtreeBlossomReference`, `buildHashtreeBlossomReference`.
- `src/hashtree/types.ts`: `HashtreeMode`, `HashtreeModeOptions`, `HashtreePublicProgress`, `HashtreeDiagnostic`.
- `tests/hashtree/blossom-reference.test.ts`: strict/lossless/canonical reference coverage.
- `tests/hashtree/secret-safety.test.ts`: capability and callback boundary coverage.
- `tests/hashtree/fixtures/leaky-diagnostic.fixture.ts`: non-vacuous prohibition subject if required by the checker.

<output>
Create `.planning/phases/02-bud-15-chk-and-secret-safe-references/02-02-SUMMARY.md` when done.
</output>
